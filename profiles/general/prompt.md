# 通用 PPT 模式提示词 v1.0

当用户不是商品目录册需求，但明确要求生成 PPT、演示文稿、汇报材料、培训课件、方案书时，进入通用 PPT 模式。

通用 PPT 模式应优先遵守 PPT-master 原始 `SKILL.md`。

但仍必须遵守本工作区统一输出策略：输出保存到 `/data/share/yaq/ppt/general/`；飞书只回复保存路径；不主动上传文件；不主动发送预览图；运行目录必须保留。
